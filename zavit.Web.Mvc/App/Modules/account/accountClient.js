(function ($) {
    $.accountClient = function (options) {
        var opts = {

        };

        $.extend(opts, options);

        return {
            getToken: function() {}
        }
    }
}(jQuery));