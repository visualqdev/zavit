(function ($) {
    $.accountService = function (options) {
        var opts = {
        };

        $.extend(opts, options);

        function logIn(email, password) {
            
        };

        return {
            logIn: logIn
        }
    }
}(jQuery))